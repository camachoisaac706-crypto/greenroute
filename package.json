{
  "name": "greenroute",
  "private": true,
  "description": "GreenRoute full stack — single-server build that serves the API and the web app from one URL.",
  "scripts": {
    "build": "npm --prefix greenroute-frontend install && npm --prefix greenroute-frontend run build && npm --prefix greenroute-backend install && node greenroute-backend/scripts/copy-frontend.mjs",
    "start": "npm --prefix greenroute-backend start"
  },
  "engines": { "node": ">=18" }
}
